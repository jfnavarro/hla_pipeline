* Make the variant calling pipelines work in parallel (by chromosome)* Add better logging and error handling (general)* Create docker container* Improve rnaseq_pipeline (add HaplotypeCaller, remove varscan in pileup mode, better filtering)* Better filtering for exome_pipeline in RNA mode* Parse tabulated data with Pandas instead of line by line* Make the exome pipeline compatible with WGS