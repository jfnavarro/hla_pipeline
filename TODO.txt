* Make the variant calling pipelines work in parallel (by chromosome)* Add better logging and error handling (general)* Create docker container* Parse tabulated data with Pandas instead of line by line* Remove intermediate files with a flag option to keep them* Remove temp files when finished* Use snpEff for annotation* Add QC steps (bamQC, fastQC, vcftools)* Use jacquard merge instead of GATK3* Add tests